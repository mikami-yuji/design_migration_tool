# Changelog

このプロジェクトの全ての注目すべき変更はこのファイルに記録されます。

## [1.2.1] - 2026-05-30

### 修正
- `main.py` の `_on_new_file_detected` コールバックにて、`import os` が漏れていたため `NameError: name 'os' is not defined` が発生し、新しいファイルが検知されてもポップアップが表示されず監視が反応しないバグを修正。
- アプリケーション全体およびコールバックの協調動作を検証するメインテストファイル `tests/test_main.py` を追加。

## [1.2.0] - 2026-05-29

### 追加
- 新しいファイル検知時の確認ダイアログ（はい・いいえ画面）にPDF用紙プレビュー表示機能を追加
  - `PyMuPDF` (fitz) を採用し、PDFの1ページ目を高品質・高精細に画像としてレンダリングするヘルパー関数を導入
  - 一般画像ファイル（PNG、JPEG、BMP、GIF）の直接プレビュー表示にも対応
  - 確認ダイアログを `900x600` のモダンな2カラム（左右分割）レイアウトへ大幅に刷新
  - ファイル破損や暗号化、ライブラリ欠損時にクラッシュせず安全に「プレビューを表示できません」と表示するフォールバック処理を実装
  - `requirements.txt` および `build_portable.bat` に `pymupdf` 依存関係を追加
  - プレビュー機能およびダイアログ制御に関する包括的な単体テストを追加（カバレッジ81%を達成）

## [1.1.3] - 2026-05-29

### 修正
- PyInstallerでビルドした実行ファイル（EXE）を実行した際、設定・履歴・ログファイルがWindowsの一時フォルダに保存され、起動するたびに設定が消える（空欄の設定画面が表示される）重大なバグを修正
  - `config_manager.py`、`history_manager.py`、および `logger.py` について、`sys.frozen` 判定を組み込み、実行パスに応じた動的なベースディレクトリの解決を導入
- 既存の設定の自動引き継ぎ（移行）措置を実装
  - 親ディレクトリ（プロジェクトルート）に既存の `config.json` がある場合、EXEのあるフォルダへ自動的に設定内容をコピーして引き継ぐロジックを追加し、ユーザーの手動再入力を不要に

## [1.1.2] - 2026-05-28

### 修正
- ユーザーが「いいえ」を押したのにファイルが移行されるように見える、二重ポップアップのバグを修正
  - コピー/移動によって監視フォルダ外へファイルが移動（削除）された際の `on_moved` イベントが誤って再検知される問題を解消
  - `PDFFileHandler` に監視対象フォルダのパスを持たせ、移動先が監視フォルダの外である場合はイベントを無視するチェックを追加

## [1.1.1] - 2026-05-28

### 修正
- 同じファイルが複数回検知されてポップアップが重複表示されるバグを修正
  - 最終更新日時 (mtime) の浮動小数点精度による JSON シリアライズ時の丸め誤差を防ぐため、10ミリ秒単位（小数点以下2桁）での丸め処理を導入
  - 重複排除（デバウンス）の時間判定をポップアップ排他ロックの外側に移動し、ユーザーのボタン操作待ち時間によってデバウンスが無効化される問題を解消

## [1.1.0] - 2026-05-28

### 追加
- ポータブルPython同梱ZIP配布パッケージを自動作成するビルドスクリプト (`build_portable.bat`) を追加
- `README.md` に開発者向けのビルド手順および一般社員向けの利用手順を詳細に追記

## [1.0.0] - 2026-05-28

### 追加
- 初回リリース
- フォルダ監視機能（watchdog使用）
- PDF検知時のポップアップ確認ダイアログ
- ファイル自動移動機能（同名ファイルリネーム対応）
- 初回起動時のGUI設定ダイアログ（フォルダ選択）
- config.jsonによる設定管理
- システムトレイ常駐機能（pystray使用）
- 監視の開始/停止切り替え
- ログ出力（ファイル + コンソール）
- ユニットテスト（pytest）
- ワンクリックで簡単に起動できるWindowsバッチファイル（起動する.bat）
- 二重ポップアップを防止し、データのやり直し（上書き）を検知する履歴管理機能（history.json）
- 元の場所にもデータを残すようにファイルを「移動」から「コピー」に変更



